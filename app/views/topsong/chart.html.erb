<div class="header">
  <h1> Top Songs</h1>
<p> Here are the top songs of the week! </p>
<p> (Provided by LastFM) </p>
</div>

  <div class="chartlist">
    <div id="songschart">
  <% @results.each do |x| %>
  <ul ="songschart">
  <li class="img"><span class="chartnumber"><%= @counter %>: </span> <a href="<%=x['url']%>"> <img class="artistimg" src="<%= x['image'].first['#text']%>" /> </a> </li>
    <li> Artist:  <%= x['artist']['name'] %> </li>
    <li> Song: <%= x['name'] %> </li>
  <% @counter +=1 %>
  <% break if @counter == 25 %>
</ul>
  <% end %>
</div>
</div>
